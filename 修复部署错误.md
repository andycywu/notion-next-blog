# 🔧 修复部署错误

## ❌ 当前问题

从构建日志中可以看到：

```
Executing user deploy command: npx wrangler deploy
✘ [ERROR] It looks like you've run a Workers-specific command in a Pages project.
For Pages, please run `wrangler pages deploy` instead.
```

**问题原因：**
- Cloudflare Pages 项目使用了错误的部署命令
- `wrangler deploy` 是用于 Workers 的命令，不是 Pages 的命令
- **实际上，Cloudflare Pages 不需要部署命令，它会自动部署**

---

## ✅ 解决方案

### 方法一：移除部署命令（推荐）

**Cloudflare Pages 会自动部署构建输出，不需要额外的部署命令。**

1. **访问 Cloudflare Dashboard**
   - 打开：https://dash.cloudflare.com/
   - 进入：**Pages** > **notion-next-blog** > **Settings**

2. **检查构建设置**
   - 进入 **Builds & deployments** 部分
   - 查看是否有 **Deploy command** 或类似的设置

3. **移除部署命令**
   - 如果看到 **Deploy command** 字段，**将其留空或删除**
   - 或者确保只有以下设置：
     - **Build command**: `npm install && npm run build`
     - **Build output directory**: `.next`
     - **Deploy command**: （留空，不填写任何内容）

4. **保存设置**
   - 点击 **Save** 保存更改

5. **重新部署**
   - 进入 **Deployments** 页面
   - 点击最新部署的 **...** 菜单
   - 选择 **Retry deployment**

---

### 方法二：如果必须保留部署命令

如果您确实需要自定义部署命令（通常不需要），应该使用：

```
wrangler pages deploy .next --project-name=notion-next-blog
```

但是，**强烈建议使用方法一**，因为 Cloudflare Pages 会自动处理部署。

---

## 📋 正确的构建设置

在 Cloudflare Dashboard 中，您的构建设置应该是：

| 设置项 | 值 | 说明 |
|--------|-----|------|
| **Framework preset** | `Next.js` | 框架预设 |
| **Build command** | `npm install && npm run build` | 构建命令 |
| **Build output directory** | `.next` | 构建输出目录 |
| **Root directory** | `/` | 根目录 |
| **Deploy command** | （留空）| **不要填写任何内容** |

---

## 🔍 如何检查当前设置

1. 访问 Cloudflare Dashboard
2. 进入 **Pages** > **notion-next-blog** > **Settings**
3. 查看 **Builds & deployments** 部分
4. 检查是否有 **Deploy command** 字段
5. 如果有，确保它是空的

---

## ✅ 修复后的预期结果

修复后，构建日志应该显示：

```
Success: Build command completed
Success: Deployment completed
```

而不是：

```
Executing user deploy command: npx wrangler deploy
✘ [ERROR] ...
```

---

## 🚀 修复步骤总结

1. ✅ 访问 Cloudflare Dashboard
2. ✅ 进入 Pages > notion-next-blog > Settings
3. ✅ 检查 Builds & deployments 设置
4. ✅ **删除或清空 Deploy command 字段**
5. ✅ 保存设置
6. ✅ 重新部署项目

---

## 📝 注意事项

- **Cloudflare Pages 会自动部署**：构建完成后，Cloudflare 会自动将 `.next` 目录的内容部署到 CDN
- **不需要手动部署命令**：与 Workers 不同，Pages 不需要 `wrangler deploy` 命令
- **构建命令就足够了**：只需要 `npm install && npm run build` 即可

---

**修复后，您的网站应该可以正常部署了！** 🎉

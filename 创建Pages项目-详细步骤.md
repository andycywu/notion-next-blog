# 📝 创建 Cloudflare Pages 项目 - 详细步骤

## 🎯 目标

创建一个新的 **Cloudflare Pages** 项目（不是 Workers），用于部署您的 Next.js 博客。

---

## 📋 详细步骤

### 步骤 1：访问 Cloudflare Dashboard

1. 打开浏览器
2. 访问：**https://dash.cloudflare.com/**
3. 使用您的账号登录：`andywu719@gmail.com`

### 步骤 2：进入 Pages（重要：不是 Workers！）

1. 在左侧菜单，找到 **"Workers 和 Pages"**（或 "Workers & Pages"）
2. 点击展开这个部分
3. **重要：点击 "Pages"**（不是 "Workers"）
   - 您应该看到 "Pages" 和 "Workers" 两个选项
   - **选择 "Pages"**

### 步骤 3：创建新项目

1. 在 Pages 页面，点击 **"Create a project"**（创建项目）按钮
2. 选择 **"Connect to Git"**（连接到 Git）
   - 不要选择 "Upload assets"（上传资源）

### 步骤 4：授权 GitHub 访问

1. 如果首次使用，会要求授权 Cloudflare 访问 GitHub
2. 点击 **"Authorize Cloudflare"**（授权 Cloudflare）
3. 选择您要授权的仓库（或选择所有仓库）
4. 点击 **"Install & Authorize"**（安装并授权）

### 步骤 5：选择 Git 仓库

1. 在仓库列表中，找到并选择：**`andycywu/notion-next-blog`**
2. 点击 **"Begin setup"**（开始设置）

### 步骤 6：配置构建设置

在项目设置页面，配置以下信息：

#### 基本信息

- **Project name**（项目名称）：`notion-next-blog`
- **Production branch**（生产分支）：`main`

#### 构建设置（重要！）

1. **Framework preset**（框架预设）：
   - 点击下拉菜单
   - **选择 "Next.js"**
   - 这是**最关键**的设置！

2. **Build command**（构建命令）：
   - 输入：`npm install && npm run build`
   - 或者：`npm run build`（如果依赖已安装）

3. **Build output directory**（构建输出目录）：
   - 输入：`.next`
   - 这是 Next.js 的默认输出目录

4. **Root directory**（根目录）：
   - 输入：`/`
   - 或者留空（默认就是根目录）

5. **Deploy command**（部署命令）：
   - **留空，不要填写任何内容！**
   - Pages 会自动部署，不需要部署命令

**重要提示：**
- ✅ 必须选择 **Framework preset: Next.js**
- ✅ **不要**填写 Deploy command
- ✅ **不要**填写 Version command（Pages 没有这个字段）

### 步骤 7：设置环境变量

1. 在构建设置页面，找到 **"Environment variables"**（环境变量）部分
2. 点击 **"Add variable"**（添加变量）

**添加必需变量：**

**变量 1：NOTION_PAGE_ID**
- **变量名**：`NOTION_PAGE_ID`
- **值**：`02ab3b8678004aa69e9e415905ef32a5`
- **环境**：选择 **Production**（或 **Apply to all environments**）
- **类型**：**Plain text**
- 点击 **Save**

**变量 2：LinkedIn 链接（推荐）**
- **变量名**：`NEXT_PUBLIC_CONTACT_LINKEDIN`
- **值**：`https://www.linkedin.com/in/andycywu/`
- **环境**：**Production**
- **类型**：**Plain text**
- 点击 **Save**

### 步骤 8：部署

1. 检查所有设置是否正确
2. 点击 **"Save and Deploy"**（保存并部署）按钮
3. 等待构建完成（约 3-5 分钟）
   - 您可以在 Dashboard 中查看构建日志
   - 构建过程中会显示进度

### 步骤 9：访问您的网站

构建完成后：

1. 您会获得一个 `*.pages.dev` 的域名
   - 例如：`https://notion-next-blog.pages.dev`
2. 访问这个 URL
3. 确认网站正常显示

---

## ✅ 验证清单

创建 Pages 项目后，确认：

- [ ] 项目类型是 **Pages**（不是 Workers）
- [ ] URL 是 `*.pages.dev`（不是 `*.workers.dev`）
- [ ] Framework preset 设置为 **Next.js**
- [ ] Build command: `npm install && npm run build`
- [ ] Build output directory: `.next`
- [ ] **Deploy command 留空**
- [ ] **没有 Version command**（Pages 没有这个）
- [ ] 环境变量已设置（`NOTION_PAGE_ID` 等）
- [ ] 构建成功完成
- [ ] 网站可以正常访问

---

## 🔍 如何区分 Pages 和 Workers

### Pages 项目 ✅

- URL: `dash.cloudflare.com/.../pages/view/notion-next-blog`
- 设置页面：显示 "Pages" 相关选项
- 没有 "Version command" 字段
- 有 "Framework preset" 选项
- 域名：`*.pages.dev`

### Workers 项目 ❌

- URL: `dash.cloudflare.com/.../workers/services/view/notion-next-blog`
- 设置页面：显示 "Worker" 相关选项
- 有 "Version command" 字段
- 没有 "Framework preset" 选项
- 域名：`*.workers.dev`

---

## 🚨 常见错误

### 错误 1：创建了 Workers 项目

**症状：**
- URL 包含 `workers/services`
- 有 "Version command" 字段
- 域名是 `*.workers.dev`

**解决：**
- 删除 Workers 项目
- 按照本指南创建 Pages 项目

### 错误 2：没有选择 Framework preset

**症状：**
- 构建失败
- 找不到 Next.js 配置

**解决：**
- 在设置中选择 **Framework preset: Next.js**

### 错误 3：填写了 Deploy command

**症状：**
- 部署失败
- 显示 Workers 相关错误

**解决：**
- 清空 Deploy command 字段
- Pages 会自动部署

---

## 📞 需要帮助？

如果遇到问题：

1. 确认您创建的是 **Pages** 项目
2. 检查 Framework preset 是否选择了 **Next.js**
3. 查看构建日志中的错误信息
4. 参考：[Workers与Pages的区别和修复.md](./Workers与Pages的区别和修复.md)

---

**按照以上步骤操作，您的 Next.js 博客应该可以成功部署到 Cloudflare Pages 了！** 🎉
